Current Free mode only. The prior fixes repaired image delivery but not catalog image coverage. Perform a catalog-wide artist-photo remediation for both /artists cards and /artist/:slug profile heroes. Verified production examples with no artist image despite established identities: Aldo Trujillo, Alejandro Fernández, and Ana Bárbara render only initials in /artists; Alejandro Fernández also has no profile hero photo while album/video artwork exists. Diagnose every one of the 563 public canonical profiles and distinguish: valid stored artist image, broken/unreachable image, provider placeholder/generic image, wrong-artist image, and truly missing image. Build one canonical server-side artist-image resolver/cache used by both directory and profile pages. Resolve from existing trusted identifiers and provider metadata in priority order (stored canonical/profile image, Songstats/Spotify artist image when available, Deezer artist image, iTunes artist result, other already integrated authoritative identity source); validate HTTP status/content-type/dimensions and reject known placeholders, cover art when an artist portrait exists, and mismatched identities. Route all external images through the existing same-origin proxy. Persist/cache successful resolutions so every page refresh does not re-query providers. If a profile truly has no verified artist image after all sources, keep an honest initial fallback and include it in a generated coverage report—never use album/video artwork as the artist portrait unless it is the authoritative artist image from a provider. Add a full-catalog audit endpoint/script/test reporting coverage totals and exact unresolved slugs, and tests ensuring directory and profile resolve the same image URL. Specifically verify Aldo Trujillo, Alejandro Fernández, Ana Bárbara, 5050 Flow Malandro, and profiles later in pagination. Run both typechecks, full image coverage audit, production build/route audit, and visual checks on directory plus profiles. Do not publish; Codex will publish after verification.