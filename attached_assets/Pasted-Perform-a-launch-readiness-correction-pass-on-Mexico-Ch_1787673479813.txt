Perform a launch-readiness correction pass on Mexico Charts based on this verified live audit. Preserve secrets, deployment/database settings, unrelated features, and the approved MX100 Spotify 60% / YouTube 40% formula. Do not publish.

1. Ranking integrity: MX100 live ranks are Fuerza Regida #1, Peso Pluma #2, Grupo Firme #6. The compare page currently labels Spotify positions as “MX100 #2/#4”; fix the source/label so it never misrepresents Spotify rank as MX100. Artist profile badges such as Grupo Firme “#18 en México” must either use the current MX100 result from the same shared calculation or explicitly say “Spotify México #18”; never leave an ambiguous/conflicting Mexico rank. Prefer a shared rank-source helper so labels cannot drift.
2. Catalog counts: homepage says 559, directory/sources 589, contribute 529. Determine why these sets differ. Do not fabricate one number. Give each metric an accurate qualifier (catalog profiles, profiles with audience data, editable/active records) or derive a common shared catalog count where they are meant to match. Ensure Spanish and English labels explain the distinction.
3. Duplicate identities: non-destructively consolidate confirmed aliases. At minimum audit Banda El Recodo vs Banda El Recodo de Cruz Lizárraga and Banda MS de Sergio Lizárraga vs Banda Sinaloense MS de Sergio Lizárraga using linked Spotify/YouTube/MusicBrainz identifiers. For confirmed same projects, keep the strongest canonical profile, redirect alias slugs, exclude aliases from directory/search/sitemap/rank pools, and preserve underlying data. Do not merge merely similar solo/group careers.
4. MX100 count: the current chart has 84 eligible artists but is branded MX100. Preserve current eligibility and formula. Make copy accurately state “up to 100” / active eligible positions so 84 does not look like a broken list. Do not manufacture entries.
5. Momentum integrity: investigate the impossible Banda Sinaloense MS weekly Spotify value (~598.2M with ~698K daily), and repeated snapshot dates. Correct the aggregation/source-field logic. Add validation so impossible or insufficient weekly/30-day totals render as unavailable instead of misleading values. Recheck Peso Pluma and Banda El Recodo examples.
6. Charts hub: the #1 YouTube artist card shows “—” with 63,025,654 views. Resolve and display Fuerza Regida from the same matched artist identity used elsewhere.
7. English polish: remove mixed-language UI in English mode, including “días”, “Mexicano/a”, and obvious untranslated MX100/home labels. Preserve artist names and proper nouns.
8. Loading/performance: static About should not depend on heavy global data before rendering. Improve Contribute loading if safely possible. The Artists page currently renders about 544 images and an enormous mobile DOM; add safe progressive rendering/pagination or a Load More pattern while preserving search/filter/sort and crawlable directory behavior.
9. Recheck charts-hub product language so fanbase/touring cards are clearly separate products and cannot be mistaken for MX100 inputs. Do not remove Radar's own fanbase metric.

Run typecheck and production build. Test desktop and 390px mobile for /, /mx100, /compare, /artists, the canonical/alias artist URLs, /charts, /acerca-de, /contribuir. Report exact fixes, remaining concerns, and observed ranks/count labels. Do not publish.